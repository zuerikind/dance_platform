---
phase: 01-trial-stability-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-trial-stability-support/CRITICAL_BUGS.md
  - src/legacy.js
  - src/scanner.js
  - src/state.js
  - src/main.js
  - supabase/migrations/*.sql
autonomous: true
must_haves:
  truths:
    - "Critical bugs reported during trial are fixed"
    - "Previously broken flows work as expected"
    - "Attendance and payment flows pass verification"
  artifacts:
    - path: .planning/phases/01-trial-stability-support/CRITICAL_BUGS.md
      provides: "List of critical bugs to fix (or empty)"
    - path: docs/VERIFICATION_CHECKLIST.md
      provides: "Post-fix verification steps"
  key_links:
    - from: "bug fix (attendance/payment/auth)"
      to: "docs/VERIFICATION_CHECKLIST.md"
      via: "run relevant checklist steps after change"
      pattern: "§2 Auth|§6 School admin"
---

<objective>
Fix all critical bugs listed for the trial so previously broken flows work and attendance/payment remain reliable.

Purpose: Deliver TRIAL-01; trial cannot be stable until critical defects are resolved.
Output: CRITICAL_BUGS.md (or updated) with all items addressed; fixes applied; verification run for affected areas.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-trial-stability-support/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.cursor/rules/live-safety.mdc
@.cursor/rules/supabase-migrations.mdc
@docs/VERIFICATION_CHECKLIST.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ensure critical bug list and fix each bug</name>
  <files>.planning/phases/01-trial-stability-support/CRITICAL_BUGS.md, src/legacy.js, src/scanner.js, src/state.js, src/main.js, supabase/migrations/*.sql</files>
  <action>
1. Create or open .planning/phases/01-trial-stability-support/CRITICAL_BUGS.md. If it does not exist, create it with a short header and seed it with the known bug from CONCERNS.md: "Student signup error message loss (src/legacy.js ~8919 – catch swallows error; user sees generic 'Unexpected signup error')". Add a line that additional trial-reported bugs can be listed one per line (description + file/location). If the file already exists and has content, use it as the list.

2. For each bug in the list:
   - Implement the fix. Use safe patterns: when changing any RPC signature, in the same migration DROP the old function (exact signature) then CREATE the new one; use optional parameters with DEFAULT NULL when extending. When fixing code that has empty or generic catch blocks, replace with at least console.warn and, for user-facing errors, surface the actual message (sanitized) or a clearer fallback (see CONCERNS.md "Empty catch blocks").
   - If the fix touches attendance, payment, or auth: run the relevant parts of docs/VERIFICATION_CHECKLIST.md (e.g. §2 Auth and §6 School admin and existing flows). If the change touches school-gated logic (Aure vs non-Aure), verify both paths.
   - Do not add new RPC overloads; do not leave two signatures for the same function name.
  </action>
  <verify>Each listed bug has a corresponding code/migration change; no new RPC overloads; npm run build succeeds.</verify>
  <done>All bugs in CRITICAL_BUGS.md are addressed; fixes follow live-safety and migration rules.</done>
</task>

<task type="auto">
  <name>Task 2: Run verification checklist for critical paths</name>
  <files>docs/VERIFICATION_CHECKLIST.md</files>
  <action>
After all bug fixes: run through docs/VERIFICATION_CHECKLIST.md §2 (Auth and profile) and §6 (School admin and existing flows—login, students list, check-in/packages/QR). Confirm: QR scan → confirm attendance → success and class deducted; create payment request → appears in list → update status. If any fix this plan touched school-gated logic, test at least one Aure and one non-Aure path. Document pass/fail or skipped items in .planning/phases/01-trial-stability-support/01-01-VERIFY.md (short bullet list).
  </action>
  <verify>01-01-VERIFY.md exists and records checklist results for §2 and §6; attendance and payment flows noted as passing or with issues.</verify>
  <done>Critical paths verified; any failures documented for follow-up.</done>
</task>

</tasks>

<verification>
- No RPC has two overloads; migrations that change a function signature include DROP of the old signature.
- Empty or generic catch blocks in fixed code are replaced with logging and/or user-visible messages.
- VERIFICATION_CHECKLIST §2 and §6 (and attendance/payment steps) completed and documented.
</verification>

<success_criteria>
- Every bug in CRITICAL_BUGS.md is fixed.
- Build passes; attendance and payment flows verified per checklist.
- Trial-critical flows work as expected.
</success_criteria>

<output>
After completion, create .planning/phases/01-trial-stability-support/01-01-SUMMARY.md
</output>
